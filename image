pipeline(){
    agent{
        label 'node2'
    }
    stages(){
        stage('build'){
            steps{
                sh '''
                docker build -d i2 /root/docker 
                '''
            }
        }
     }
  }
