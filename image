pipeline(){
    agent any{
        label 'node2'
    }
    stages(){
        stage('build'){
            steps{
                sh '''
                docker build -t i2 /root/docker 
                '''
            }
        }
     }
  }
